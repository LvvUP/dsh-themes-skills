name: One isolated ID-less Plugin replacement runtime task
description: Runs one candidate key on one exact alpha.2 platform and Node tuple

inputs:
  batch-id:
    required: true
  candidate-key:
    required: true
  candidate-token:
    required: true
  repository:
    required: true
  commit:
    required: true
  tree:
    required: true
  tuple:
    required: true
  node-version:
    required: true

runs:
  using: composite
  steps:
    - name: Check out the exact untrusted replacement candidate without credentials
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      with:
        repository: ${{ inputs.repository }}
        ref: ${{ inputs.commit }}
        path: .ci/replacement-candidate
        fetch-depth: 1
        persist-credentials: false

    - name: Check out the exact official alpha.2 source without credentials
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      with:
        repository: deepseek-ai/deepseek-harness
        ref: dsh-v0.1.2-alpha.2
        path: .ci/upstream
        fetch-depth: 1
        fetch-tags: true
        persist-credentials: false

    - name: Install the exact tuple Node
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: ${{ inputs.node-version }}
        cache: npm
        cache-dependency-path: authority/package-lock.json

    - name: Install verifier dependencies without lifecycle scripts
      shell: bash
      working-directory: authority
      run: npm ci --ignore-scripts

    - name: Verify exact candidate and Harness Git identities
      shell: bash
      run: |
        test "$(git -C .ci/replacement-candidate rev-parse HEAD)" = "${{ inputs.commit }}"
        test "$(git -C .ci/replacement-candidate rev-parse HEAD^{tree})" = "${{ inputs.tree }}"
        test "$(git -C .ci/upstream rev-parse HEAD)" = "0a53fb55bea101816fa226bb964ae2bed71c343b"
        test "$(git -C .ci/upstream rev-parse HEAD^{tree})" = "64ccbfa8e0caa4711cd4a75717ef9e022657961b"
        node authority/skills/dsh-harness-installer/scripts/verify-source.mjs \
          --source "${{ github.workspace }}/.ci/upstream"

    - name: Build the exact official alpha.2 source for this tuple
      shell: bash
      run: |
        node authority/skills/dsh-harness-installer/scripts/build-source.mjs \
          --source "${{ github.workspace }}/.ci/upstream" \
          --receipt "${{ runner.temp }}/alpha2-private-build-receipt.json" >/dev/null

    - name: Require a reviewed candidate-specific executor
      shell: bash
      run: |
        node authority/skills/dsh-plugin-installer/scripts/plugin-replacement-runtime-fixed-executor.mjs \
          --candidate-key "${{ inputs.candidate-key }}" \
          --candidate-source "${{ github.workspace }}/.ci/replacement-candidate" \
          --harness-source "${{ github.workspace }}/.ci/upstream" \
          --build-receipt "${{ runner.temp }}/alpha2-private-build-receipt.json" \
          --artifact-output "${{ runner.temp }}/replacement-candidate-artifact.tgz" \
          --output "${{ runner.temp }}/replacement-runtime-stage-evidence.json" \
          --tuple "${{ inputs.tuple }}"

    - name: Seal only sanitized passed-stage evidence
      shell: bash
      run: |
        mkdir -p "${{ runner.temp }}/replacement-runtime-receipt"
        node authority/skills/dsh-plugin-installer/scripts/plugin-replacement-runtime-certification.mjs seal \
          --artifact "${{ runner.temp }}/replacement-candidate-artifact.tgz" \
          --candidate-source "${{ github.workspace }}/.ci/replacement-candidate" \
          --evidence "${{ runner.temp }}/replacement-runtime-stage-evidence.json" \
          --output "${{ runner.temp }}/replacement-runtime-receipt/${{ inputs.candidate-token }}-${{ inputs.tuple }}.json" \
          --workflow "${{ github.workspace }}/authority/.github/workflows/alpha2-plugin-replacement-runtime-certification.yml" \
          --candidate-key "${{ inputs.candidate-key }}" \
          --tuple "${{ inputs.tuple }}"

    - name: Upload only this task's sanitized receipt
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: replacement-runtime-${{ github.run_id }}-${{ github.run_attempt }}-${{ inputs.candidate-token }}-${{ inputs.tuple }}
        path: ${{ runner.temp }}/replacement-runtime-receipt/${{ inputs.candidate-token }}-${{ inputs.tuple }}.json
        if-no-files-found: error
        retention-days: 7
        compression-level: 9
