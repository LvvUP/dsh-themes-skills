interface:
  display_name: "DSH Theme Finder"
  short_description: "Classify hosted, community, and showcase directory records"
  default_prompt: "Use $dsh-theme-finder to resolve my unique DSH-Themes #ID. Derive and validate every package, version, URL, and digest internally; never ask me to provide them. Treat names and detail URLs as discovery-only, and hand off only records accepted by the pinned certified baseline. Keep DSH setup separate; RC.2 remains pending with zero installable results."
