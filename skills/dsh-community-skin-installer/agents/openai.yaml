interface:
  display_name: "DSH Community Skin Installer"
  short_description: "Install allowlisted skins under pinned runtime evidence"
  default_prompt: "Use $dsh-community-skin-installer only after official DSH has completed a separate first-start task; never install DSH or Node here. Resolve exact package, artifact, and digest evidence internally and, after consent, install only when the pinned certified Manager and item receipt both validate; RC.2 never authorizes installation."
