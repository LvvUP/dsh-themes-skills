interface:
  display_name: "DSH Community Skin Installer"
  short_description: "Install allowlisted skins under pinned runtime evidence"
  default_prompt: "Use $dsh-community-skin-installer only after official DSH has completed a separate first-start task and Finder has validated the exact public #ID shown at the top-left of the selected card or detail page. Reject legacy DSH-* labels and every cross-item mismatch. Keep package, version, artifact, and digest coordinates internal; after consent, install only when the pinned Manager and item receipt both validate. RC.2 remains pending and never authorizes installation."
