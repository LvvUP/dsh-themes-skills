interface:
  display_name: "DSH Community Skin Installer"
  short_description: "Install allowlisted skins under pinned runtime evidence"
  default_prompt: "Use $dsh-community-skin-installer to inspect and, after explicit consent, install an allowlisted community skin only when the pinned certified Manager and item receipt both validate; the RC.2 receipt is pending and never authorizes installation."
