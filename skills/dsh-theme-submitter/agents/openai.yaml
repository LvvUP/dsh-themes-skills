interface:
  display_name: "DSH Theme Submitter"
  short_description: "Validate a theme and open the secure submission flow"
  default_prompt: "Use $dsh-theme-submitter to validate my data-only V3 theme against the pinned certified submission sidecar and guide me to the website; never submit from the pending RC.2 lane."
