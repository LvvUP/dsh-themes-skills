interface:
  display_name: "DSH Theme Manager"
  short_description: "Safely install, verify, and roll back DSH themes"
  default_prompt: "Use $dsh-theme-manager to validate and install a verified DSH-Themes V2 package."
