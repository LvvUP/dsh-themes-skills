interface:
  display_name: "DSH Theme Manager"
  short_description: "Safely install, switch, verify, and roll back DSH themes"
  default_prompt: "Use $dsh-theme-manager to safely install and verify a DSH-Themes package."
