interface:
  display_name: "DSH Theme Manager"
  short_description: "Safely install, verify, and roll back DSH themes"
  default_prompt: "Use $dsh-theme-manager with the pinned certified baseline policy to validate and install a hosted V3 package, then perform a managed cold restart and digest-bound recoverable rollback; inspect the RC.2 candidate only as pending evidence and never install from it."
