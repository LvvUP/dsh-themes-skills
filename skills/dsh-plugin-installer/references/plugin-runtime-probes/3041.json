{
  "schemaVersion": 1,
  "purpose": "dsh-plugin-alpha1-fixed-runtime-probe",
  "authorityEffect": "contract-only-not-runtime-authority",
  "candidateExecuted": false,
  "catalogId": 3041,
  "baseline": {
    "tag": "dsh-v0.1.2-alpha.1",
    "commit": "cd5ef8148158c3a752a658978873241fdf8e2bbc",
    "tree": "a712eec535b48badc4fefb4df5176a7002e4280b"
  },
  "package": {
    "name": "@dsh-themes/context-vista",
    "version": "0.1.0-dsh.alpha1.1",
    "profile": "web",
    "cordisEntryId": "dsh-plugin-3041-context-vista"
  },
  "capabilities": {
    "permissions": [
      "read the current session tokenUsage, contextPressure, and contextBreakdown standard projections"
    ],
    "network": [],
    "processes": [],
    "files": [],
    "clientServices": [
      "slots",
      "locale"
    ],
    "remoteMethods": [],
    "browserPersistence": "none"
  },
  "combination": {
    "additiveSlots": [
      "conversation.input.dock:context-vista"
    ],
    "exclusiveResources": [],
    "officialSurfacePreserved": true
  },
  "assertions": [
    {
      "id": "package.exact-entry",
      "class": "package-graph",
      "expected": "The exact package and Cordis entry occur once; every declared DeepSeek peer resolves to 0.1.2-alpha.1 and no removed aggregate runtime resolves."
    },
    {
      "id": "boot.ready-context-once",
      "class": "cold-web-boot",
      "expected": "A cold Web start reaches __DSH_BOOT_READY__; one context-vista dock and one owned style are present with no Host behavior."
    },
    {
      "id": "feature.projection-meter",
      "class": "feature-contract",
      "expected": "Fixed projection fixtures render bounded occupancy, remaining capacity, three composition values, and four durable usage buckets without reconstructing event history."
    },
    {
      "id": "security.read-only-projections",
      "class": "security-boundary",
      "expected": "The feature reads only the three standard projections and performs no Remote, route, outbound request, credential, process, file, browser persistence, or code-generation operation."
    },
    {
      "id": "ui.keyboard-zoom-themes",
      "class": "accessibility-responsive",
      "expected": "The native disclosure is keyboard operable; labels, narrow layout, 200 percent zoom, 12/14/17 pixel font settings, and light and dark schemes remain legible without clipping essential values."
    },
    {
      "id": "dispose.zero-residue",
      "class": "teardown-rollback",
      "expected": "Removal leaves zero plugin slots, styles, locale dictionaries, package rows, client graph entries, or dependency-closure changes, and governed Home and Profile bytes match the snapshot."
    },
    {
      "id": "combine.additive-dock-rollback",
      "class": "combination",
      "expected": "The additive context-vista dock coexists with the official input, todo, and goal surfaces and full-batch rollback restores all prior entries after an injected later failure."
    }
  ]
}
