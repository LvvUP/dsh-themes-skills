{
  "schemaVersion": 1,
  "purpose": "dsh-plugin-alpha1-fixed-runtime-probe",
  "authorityEffect": "contract-only-not-runtime-authority",
  "candidateExecuted": false,
  "catalogId": 3006,
  "baseline": {
    "tag": "dsh-v0.1.2-alpha.1",
    "commit": "cd5ef8148158c3a752a658978873241fdf8e2bbc",
    "tree": "a712eec535b48badc4fefb4df5176a7002e4280b"
  },
  "package": {
    "name": "@dsh-themes/dsh-better-model-selector",
    "version": "1.0.0-dsh.alpha1.1",
    "profile": "web",
    "cordisEntryId": "dsh-plugin-3006-better-model-selector"
  },
  "capabilities": {
    "permissions": [
      "read the official per-session model catalog and durable current selection",
      "change the model or reasoning effort only after an explicit user action",
      "read and update one non-secret favorites setting after an explicit star-button action"
    ],
    "network": [
      "authenticated in-origin model and settings services supplied by the exact alpha.1 baseline"
    ],
    "processes": [],
    "files": [
      "the official settings provider may update DSH_HOME/settings.yaml after an explicit favorites action"
    ],
    "clientServices": [
      "slots",
      "sessions",
      "modelDirectories",
      "locale",
      "settingsScope"
    ],
    "remoteMethods": [],
    "browserPersistence": "settings-scope-only"
  },
  "combination": {
    "additiveSlots": [],
    "exclusiveResources": [
      "conversation.input.model:priority=-1",
      "settings-namespace:dsh-better-model-selector",
      "style:dsh-plugin-3006-better-model-selector/client.css"
    ],
    "officialSurfacePreserved": true
  },
  "assertions": [
    {
      "id": "package.exact-entry",
      "class": "package-graph",
      "expected": "The exact package and Cordis entry occur once; every declared DeepSeek peer resolves to 0.1.2-alpha.1, Schemastery resolves to 3.18.1, and no removed aggregate runtime resolves."
    },
    {
      "id": "boot.ready-selector-once",
      "class": "cold-web-boot",
      "expected": "A cold Web start reaches __DSH_BOOT_READY__; one owned style and one priority -1 model selector occur, while the official priority 0 fallback remains registered."
    },
    {
      "id": "feature.authoritative-selection",
      "class": "feature-contract",
      "expected": "The selector reads ModelDirectory for the active direct session, hides for addressed subagents, and reports success only after the official durable model or effort projection accepts the explicit selection."
    },
    {
      "id": "feature.confirmed-favorites",
      "class": "feature-contract",
      "expected": "Host-mode favorite writes succeed only when a post-write settings snapshot exactly confirms the requested value; memory mode stays process-local and says that Host persistence is unavailable."
    },
    {
      "id": "security.official-services-only",
      "class": "security-boundary",
      "expected": "No raw route, direct browser request, credential API, process, direct file API, global shortcut, browser storage, HTML sink, or transport-detail disclosure occurs."
    },
    {
      "id": "ui.keyboard-zoom-locales",
      "class": "accessibility-responsive",
      "expected": "Sibling model and favorite buttons, focus restoration, Escape, narrow layout, 200 percent zoom, light and dark schemes, and all registered locale dictionaries remain usable."
    },
    {
      "id": "dispose.zero-residue-fallback",
      "class": "teardown-rollback",
      "expected": "Removal leaves zero plugin slots, styles, dictionaries, settings subscriptions, listeners, package rows, client graph entries, or dependency changes; the official priority 0 selector resumes and governed state matches the snapshot."
    },
    {
      "id": "combine.exclusive-seat-rollback",
      "class": "combination",
      "expected": "Preflight rejects another non-official model-seat owner or matching settings namespace, and an injected later batch failure restores the entire ten-item transaction without partial success."
    }
  ]
}
