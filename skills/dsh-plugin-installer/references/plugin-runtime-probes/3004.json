{
  "schemaVersion": 1,
  "purpose": "dsh-plugin-alpha2-fixed-runtime-probe",
  "authorityEffect": "contract-only-not-runtime-authority",
  "candidateExecuted": false,
  "catalogId": 3004,
  "baseline": {
    "tag": "dsh-v0.1.2-alpha.2",
    "commit": "0a53fb55bea101816fa226bb964ae2bed71c343b",
    "tree": "64ccbfa8e0caa4711cd4a75717ef9e022657961b"
  },
  "package": {
    "name": "@dsh-themes/dsh-spotlight",
    "version": "0.0.2-dsh.alpha2.1",
    "profile": "web",
    "cordisEntryId": "dsh-plugin-3004-spotlight"
  },
  "capabilities": {
    "permissions": [
      "read standard session-list summaries and open one existing session after a direct user click",
      "read the official command directory for the current session and execute only a no-input slash command after a direct user click",
      "read the official Loader plugin inventory without changing Loader state",
      "register one client-only popupSelect command that opens the same local palette",
      "create, deduplicate, and remove only the plugin-owned style element"
    ],
    "network": [
      "authenticated in-origin Commands Remote directory reads and one direct-user-click command execution only",
      "authenticated in-origin Plugin Inventory Remote reads only",
      "indirect: an executed official command may use capabilities already granted by its owning Host command"
    ],
    "processes": [
      "indirect: an executed official command may use process capability already granted by its owning Host command; Spotlight starts no process"
    ],
    "files": [
      "indirect: an executed official command may use file capability already granted by its owning Host command; Spotlight reads and writes no file"
    ],
    "clientServices": [
      "slots",
      "sessions",
      "locale",
      "remote.commands",
      "remote.pluginInventory",
      "commandUi"
    ],
    "remoteMethods": [
      "commands.list",
      "commands.execute",
      "pluginInventory.list"
    ],
    "browserPersistence": "none"
  },
  "combination": {
    "additiveSlots": [
      "sidebar.footer.action:dsh-spotlight-trigger",
      "shell.overlay:dsh-spotlight-palette"
    ],
    "exclusiveResources": [],
    "officialSurfacePreserved": true
  },
  "assertions": [
    {
      "id": "package.exact-entry",
      "class": "package-graph",
      "expected": "The exact hosted package and Cordis entry occur once; every declared DeepSeek peer resolves to 0.1.2-alpha.2 and no removed aggregate runtime resolves."
    },
    {
      "id": "boot.ready-context-once",
      "class": "cold-web-boot",
      "expected": "A cold Web start reaches __DSH_BOOT_READY__; one footer trigger, one dormant overlay occupant, one owned style, one popupSelect command, and eight locale dictionaries are present."
    },
    {
      "id": "feature.official-directory-search",
      "class": "feature-contract",
      "expected": "The palette searches bounded standard-session summaries, session-bound no-input command descriptors, and read-only Plugin Inventory entries without traversing host page content or retaining browser data."
    },
    {
      "id": "feature.explicit-session-command-actions",
      "class": "feature-contract",
      "expected": "Opening an existing session and executing a no-input slash command each require a direct item click; command descriptors remain bound to the Session that produced them, stale Session results cannot execute, and a command result is reported without retry, replacement routing, or composer injection."
    },
    {
      "id": "security.official-services-only",
      "class": "security-boundary",
      "expected": "The adaptation uses only official Slots, Sessions, Locale, Commands Remote, Plugin Inventory Remote, and Command UI surfaces; its only raw DOM access creates, deduplicates, and removes its own style element, and it has no host-content traversal, storage, external request, route, credential, process, file, Agent, Session creation, or automatic command execution capability."
    },
    {
      "id": "ui.keyboard-zoom-themes",
      "class": "accessibility-responsive",
      "expected": "The popupSelect command, footer trigger, dialog, search input, close control, result controls, narrow layout, 200 percent zoom, 12/14/17 pixel font settings, and light and dark schemes remain usable."
    },
    {
      "id": "dispose.zero-residue",
      "class": "teardown-rollback",
      "expected": "Removal leaves zero owned slots, style, locale dictionaries, popupSelect command, package rows, client graph entries, or dependency-closure changes; governed Home and Profile bytes match the snapshot."
    },
    {
      "id": "combine.additive-slots-rollback",
      "class": "combination",
      "expected": "Both uniquely identified additive slots coexist with official sidebar, overlay, commands, sessions, plugin settings, todo, goal, and conversation surfaces; full-batch rollback restores all prior entries after an injected later failure."
    }
  ]
}
