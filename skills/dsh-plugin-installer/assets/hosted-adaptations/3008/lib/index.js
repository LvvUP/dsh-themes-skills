/**
 * Public plugin #3008 alpha.2 reviewed replacement — Host discovery anchor.
 *
 * View Modes is a client-only, additive Conversation View over the official
 * Chat navigation projection. Its Host half intentionally creates no route,
 * Remote, process, file, network, or persistence capability.
 */

export const name = '@dsh-themes/dsh-view-modes'
export const inject = []

export function apply() {}
