# Review evidence only: this immutable package is not installable until the
# alpha.2 runtime probe is completed and promoted by separate authority work.
- insert:
    - id: dsh-plugin-3008-view-modes
      name: '@dsh-themes/dsh-view-modes'
