/**
 * Public plugin #3011 alpha.2 reviewed replacement — Host discovery anchor.
 *
 * Arcana is a browser-only view over the official Commands directory. The
 * Host half owns no route, command, process, file, network, secret, or state.
 */

export const name = '@dsh-themes/arcana'
export const inject = []

export function apply() {}
