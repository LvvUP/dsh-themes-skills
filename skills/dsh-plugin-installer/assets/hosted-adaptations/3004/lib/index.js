/**
 * Public plugin #3004 alpha.2 reviewed replacement — Host discovery anchor.
 *
 * Spotlight is a browser-only, reversible command surface. The Host half owns
 * no route, Remote, process, file, network, credential, or persistence work.
 */

export const name = '@dsh-themes/dsh-spotlight'
export const inject = []

export function apply() {}
