/**
 * Public plugin #3050 alpha.2 reviewed replacement — Host discovery anchor.
 *
 * The adaptation deliberately has no Host behavior. Planning lives in the
 * browser; a direct user click submits either an ordinary prompt through the
 * official Session Controller or a request asking the root Agent to use
 * alpha.2's official Schedule tool. The plugin cannot constrain that choice.
 * No timer, task runner, persistence, route, process, file, network,
 * credential, Agent, Session, or direct tool capability is owned here.
 */

export const name = '@dsh-themes/dsh-automation'
export const inject = []

export function apply() {}
