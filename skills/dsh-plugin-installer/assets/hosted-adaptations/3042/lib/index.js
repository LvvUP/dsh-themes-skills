/**
 * Public plugin #3042 alpha.1 reviewed replacement — Host discovery anchor.
 *
 * The adapted feature is entirely browser-side. Workspace candidates come
 * from alpha.1's existing authenticated fileReferences Remote and selected
 * notes become standard @path references, so this Host half adds no route,
 * Remote, file, process, settings, message-boundary, or network capability.
 */

export const name = '@dsh-themes/dsh-wikilink'
export const inject = []

export function apply() {}
