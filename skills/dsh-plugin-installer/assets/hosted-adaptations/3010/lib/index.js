/**
 * Public plugin #3010 alpha.2 reviewed replacement — Host discovery anchor.
 *
 * The hosted adaptation is browser-only. It discovers path metadata through
 * alpha.2's authenticated fileReferences Remote and writes a standard @path
 * review request into the composer only after an explicit user click. It owns
 * no route, file reader, process, renderer, secret, network client, or store.
 */

export const name = '@dsh-themes/dsh-openpencil'
export const inject = []

export function apply() {}
